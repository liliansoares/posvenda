import { ColumnTableIdPipe } from './column-table-id.pipe';

describe('ColumnTableIdPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnTableIdPipe();
    expect(pipe).toBeTruthy();
  });
});
