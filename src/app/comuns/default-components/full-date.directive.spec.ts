import { FullDateDirective } from './full-date.directive';

describe('FullDateDirective', () => {
  it('should create an instance', () => {
    const directive = new FullDateDirective();
    expect(directive).toBeTruthy();
  });
});
