import { ChargebackDirective } from './chargeback.directive';

describe('ChargebackDirective', () => {
  it('should create an instance', () => {
    const directive = new ChargebackDirective();
    expect(directive).toBeTruthy();
  });
});
