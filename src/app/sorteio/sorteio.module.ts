import { NgModule } from '@angular/core';
import { SorteioRoutingModule } from './sorteio.routing.module';

@NgModule({
    declarations: [],
    imports: [SorteioRoutingModule],
    providers: []
  })

export class SorteioModule { }